export const routes = {
  home: '/',
  login: '/login',
  admin: '/admin',
  addNews: '/admin/news',
  hNews: 'h-news',
  hOffer: 'h-offer',
  hContact: 'h-contact',
  hAbout: 'h-about',
}

export const staticPath = 'express/uploads' as const
export const imagePaths = ['/images', '/blur'] as const
